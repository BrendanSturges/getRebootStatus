# Author: github.com/brendansturges
# Description: getOSSP.ps1 - This script queries a file the user chooses and checks all servers within to see if the server is in a pending reboot status and exports this to a CSV
# Requires SCCM WMI Class to run against